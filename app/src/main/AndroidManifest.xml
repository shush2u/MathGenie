<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testing">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.MathGenie">
        <activity android:name=".quadraticActivity"></activity>
        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".landing_page" />
        <activity android:name=".quadratic"/>
        <activity android:name=".coordinate_tieses" />
        <activity android:name=".coordinate_tieses_explanation" />
        <activity android:name=".pythagoras" />
        <activity android:name=".pythagoras_explanation" />
    </application>

</manifest>